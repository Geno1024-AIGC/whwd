name: build

on:
  push:
  pull_request:

jobs:
  build:
    name: build-${{ matrix.arch }}
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86
            cmake_arch: Win32
          - arch: x64
            cmake_arch: x64
    steps:
      - uses: actions/checkout@v7
      - name: Configure
        run: cmake -B build -A ${{ matrix.cmake_arch }} -DWHWD_CI_BUILD=${{ github.run_number }} -DWHWD_CI_SHA=${{ github.sha }}
      - name: Build
        run: cmake --build build --config Release
      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: whwd-${{ matrix.arch }}
          path: |
            build/Release/whwd.exe
            build/Release/whwd-gui.exe